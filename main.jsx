import React from "react";
import ReactDOM from "react-dom/client";
import MatrixExplorer from "./matrix-explorer.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MatrixExplorer />
  </React.StrictMode>
);
