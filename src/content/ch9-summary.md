# intro

You made it all the way through. Let's zoom out and look at the whole map. Every idea below is something you now have in your toolkit — and more importantly, you've seen how they all connect to each other.

The summary cards below are generated by the GUI from the chapter data. Below them, you'll find a "Big Picture" reflection and pointers to where to go next.

# outro

:::callout color=gold
**The Big Picture.** Everything here was one story. Linear equations became matrices. Matrices became transformations of space. Dot products gave us perpendicularity, lengths, and projections — and those were exactly the tools needed to discover sin and cos hiding inside rotation matrices. Determinants tied it all back to systems of equations. Homogeneous coordinates let us compose transformations. Markov chains showed that a matrix can encode not just geometry but *probability*, with a steady state that quietly introduced eigenvectors. And neural networks revealed that all of modern AI is just stacks of these same matrix transformations, with a tiny non-linear squish in between, tuned to warp space until problems become solvable.
:::end

## Where to go next

- **Matrix inverses**: the formal "undo" operation — A⁻¹ exists exactly when det ≠ 0.
- **Eigenvectors in general**: vectors the matrix only scales (not just the steady-state one).
- **3D vectors and matrices**: everything generalizes; rotation becomes 3×3, and you get cross products.
- **Matrix decompositions**: SVD and QR — the deep structural theorems of linear algebra.
- **Applications**: PageRank, computer graphics, neural networks, quantum mechanics — all linear algebra.
- **Backpropagation**: the calculus-on-matrices that lets neural networks learn from data.
